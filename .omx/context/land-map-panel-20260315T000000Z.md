Task statement
- Rework the app toward a Naver-real-estate style experience: show parcel boundaries on the map and detailed land/listing information in a side panel.
- Keep and improve the flow where users can post their own land as a listing.
- User requested `$team 2`, but tmux is unavailable in this environment, so actual OMX team mode is blocked.

Desired outcome
- Listings view centers on a map + info panel layout instead of a map widget above cards.
- Clicking a listing or a parcel boundary updates the side panel with parcel and listing details.
- My listings page makes "register my land" obvious and map-first, with selected parcel details visible beside the form.
- Existing create/update listing APIs continue to work with saved parcel geometry fields.

Known facts / evidence
- `frontend/src/components/ListingsMapPanel.tsx` already renders saved parcel geometries and a side panel for listing details.
- `frontend/src/pages/ListingsPage.tsx` still uses a top map section plus separate cards below.
- `frontend/src/pages/MyListingsPage.tsx` already supports creating/editing listings with `ParcelMapPicker`.
- `backend/src/routes/listings.ts` already stores `parcel_pnu`, `center_lat`, `center_lng`, and `parcel_geojson`.
- Live VWorld search is unstable in this environment; browsing UIs should not depend on it.

Constraints
- Avoid destructive git operations.
- Use `apply_patch` for edits.
- Keep existing saved parcel geometry behavior working.
- Do not depend on successful live VWorld responses for the core browse experience.

Unknowns / open questions
- Whether to preserve the current public listing cards under the new side panel or fold them into the panel entirely.
- Whether to add any extra owner-facing metadata fields beyond the existing parcel + listing fields.

Likely touchpoints
- `frontend/src/pages/ListingsPage.tsx`
- `frontend/src/components/ListingsMapPanel.tsx`
- `frontend/src/pages/MyListingsPage.tsx`
- `frontend/src/components/ParcelMapPicker.tsx`
- `frontend/src/index.css`
